<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="apam.Pages.RecordatorioTramitesPage"
             xmlns:vm="clr-namespace:apam.ViewModels"
             Background="White"
             Title="Recordatorio Trámites">

    <ContentPage.BindingContext>
        <vm:RecordatoriosTramiteViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <Color x:Key="Divider">LightGrey</Color>

        <DataTemplate x:Key="MenuItemTemplateHelp">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="75" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Image Source="coin.png" WidthRequest="32" Grid.Column="1"  />
                <VerticalStackLayout Grid.Column="3" VerticalOptions="Center">
                    <Label Text="{Binding Nombre}" FontSize="16" TextColor="Black" LineBreakMode="TailTruncation" FontAttributes="Bold" MaxLines="1"  />
                    <Label Text="{Binding Descripcion}" FontSize="14" TextColor="DarkGray" LineBreakMode="TailTruncation" FontAttributes="Bold" MaxLines="1"  />
                    <Label Text="{Binding HoraDescripcion}" FontSize="14" TextColor="DarkGray" LineBreakMode="TailTruncation" FontAttributes="Bold" MaxLines="1"  />
                </VerticalStackLayout>

                <BoxView Grid.Row="1" Grid.ColumnSpan="4" HeightRequest="1" BackgroundColor="{StaticResource Divider}" />

                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Tapped="TapGestureRecognizer_Tapped" CommandParameter="{Binding Id}" />
                    <TapGestureRecognizer NumberOfTapsRequired="2" Tapped="TapGestureRecognizer_DoubleTapped" CommandParameter="{Binding Id}" />
                </Grid.GestureRecognizers>
            </Grid>
        </DataTemplate>
    </ContentPage.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <HorizontalStackLayout Grid.Row="0" Margin="0" BackgroundColor="#efefef" Grid.ColumnSpan="2" >
            <Image Source="payments2.png" HeightRequest="48" WidthRequest="48" Margin="20,10,10,10" VerticalOptions="Center"/>
            <Label Text="Trámites" FontSize="16" TextColor="Black" LineBreakMode="WordWrap" FontAttributes="Bold" Grid.Column="1" VerticalOptions="Center" />
        </HorizontalStackLayout>

        <Image Source="add1.png" HeightRequest="40" WidthRequest="40" Margin="0,10,20,10" VerticalOptions="Center" Grid.Row="0" Grid.Column="1">
            <Image.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnAddRecordatorioTapped" />
            </Image.GestureRecognizers>
        </Image>

        <CollectionView Grid.Row="1" Grid.ColumnSpan="2"
                x:Name="OptionsHelpCollection" HorizontalOptions="Center"
                ItemsSource="{Binding Tramites}"
                ItemTemplate="{StaticResource MenuItemTemplateHelp}"
                SelectionMode="None">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="15"/>
            </CollectionView.ItemsLayout>
        </CollectionView>

        <VerticalStackLayout Grid.Row="2" Margin="10" Spacing="10" Grid.ColumnSpan="3">
            <BoxView HeightRequest="1" BackgroundColor="{StaticResource Divider}" Grid.Row="2" Grid.ColumnSpan="3"/>

            <!-- Already have account -->
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="6" Grid.Row="2">
                <Label Text="1 toque "/>
                <Label Text="EDITAR" FontAttributes="Bold"/>
                <Label Text="   |   "/>
                <Label Text="2 toques"/>
                <Label Text="ELIMINAR" FontAttributes="Bold"/>
            </HorizontalStackLayout>
        </VerticalStackLayout>
        <!-- Divider -->

    </Grid>

</ContentPage>