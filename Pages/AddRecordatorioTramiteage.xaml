<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="apam.Pages.AddRecordatorioTramiteage"
             xmlns:vm="clr-namespace:apam.ViewModels"
             BackgroundColor="White"
             Title="Recordatorio Trámite">

    <ContentPage.BindingContext>
        <vm:AddRecordatorioTramitesViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <Color x:Key="Primary">#044680</Color>
        <Color x:Key="Muted">#F2F3F7</Color>
        <Color x:Key="Divider">#E9EBF0</Color>
        <Thickness x:Key="ScreenPadding">20,16</Thickness>

        <Style TargetType="Label">
            <Setter Property="TextColor" Value="{AppThemeBinding Light=#0C0C0C, Dark=#F6F6F6}" />
        </Style>
        <Style x:Key="Caption" TargetType="Label">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="TextColor" Value="{AppThemeBinding Light=#8F97A3, Dark=#9BA3AF}" />
        </Style>
        <Style x:Key="TitleLarge" TargetType="Label">
            <Setter Property="FontSize" Value="22" />
            <Setter Property="FontAttributes" Value="Bold" />
        </Style>
        <Style x:Key="InputLabel" TargetType="Label" BasedOn="{StaticResource Caption}">
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{AppThemeBinding Light=#455066, Dark=#C8CFDA}"/>
        </Style>
        <Style x:Key="FormEntry" TargetType="Entry">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="TextColor" Value="{AppThemeBinding Light=#0C0C0C, Dark=#F6F6F6}"/>
            <Setter Property="PlaceholderColor" Value="{AppThemeBinding Light=#A7AFC0, Dark=#9BA3AF}"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="HeightRequest" Value="52"/>
            <Setter Property="BackgroundColor" Value="{StaticResource Primary}"/>
            <Setter Property="TextColor" Value="White"/>
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontAttributes" Value="Bold"/>
        </Style>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*">
        <!-- Top bar -->

        <!-- Content -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="18" Padding="{StaticResource ScreenPadding}">

                <!-- Header -->
                <VerticalStackLayout Spacing="6" Margin="0,4,0,8">
                    <Label Text="Crear Recordatorio" Style="{StaticResource TitleLarge}" x:Name="lblTipoTitulo"/>
                </VerticalStackLayout>

                <!-- Nombre -->
                <VerticalStackLayout Spacing="8">
                    <Label Text="Nombre Recordatorio" Style="{StaticResource InputLabel}"/>
                    <Border BackgroundColor="{StaticResource Muted}" StrokeThickness="0" StrokeShape="RoundRectangle 14" Padding="14,12">
                        <Entry Style="{StaticResource FormEntry}" Placeholder="Ingrese Nombre Recordatorio" Text="{Binding Nombre}" ReturnType="Next" MaxLength="50"/>
                    </Border>
                </VerticalStackLayout>

                <!-- Descripcion -->
                <VerticalStackLayout Spacing="8">
                    <Label Text="Descripcion" Style="{StaticResource InputLabel}"/>
                    <Border BackgroundColor="{StaticResource Muted}" StrokeThickness="0" StrokeShape="RoundRectangle 14" Padding="14,12">
                        <Entry Style="{StaticResource FormEntry}" Placeholder="Ingrese Descripcion" Text="{Binding Descripcion}" ReturnType="Next" MaxLength="50"/>
                    </Border>
                </VerticalStackLayout>

                <!-- Fecha -->
                <VerticalStackLayout Spacing="8">
                    <Label Text="Fecha" Style="{StaticResource InputLabel}"/>
                    <Border BackgroundColor="{StaticResource Muted}" StrokeThickness="0" StrokeShape="RoundRectangle 14" Padding="14,12">
                        <DatePicker Style="{StaticResource FormEntry}" Date="{Binding Fecha}" Format="dd MMMM, yyyy" />
                    </Border>
                </VerticalStackLayout>

                <!-- Hora -->
                <VerticalStackLayout Spacing="8">
                    <Label Text="Hora" Style="{StaticResource InputLabel}"/>
                    <Border BackgroundColor="{StaticResource Muted}" StrokeThickness="0" StrokeShape="RoundRectangle 14" Padding="14,12">
                        <TimePicker Style="{StaticResource FormEntry}"  x:Name="dobPicker" Time="{Binding Hora}" Format="t" />
                    </Border>
                </VerticalStackLayout>

                <!-- Submit -->
                <Button Text="Guardar Recordatorio" Style="{StaticResource PrimaryButton}" Command="{Binding RegisterCommand}" />

            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>